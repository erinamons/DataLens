# 2026-05-20 工作日志

## GitHub 上传

- 初始化本地 Git 仓库。
- 添加 `.gitignore`，排除本地运行数据、上传视频、封面、日志和构建产物。
- 提交项目初始版本并推送到 GitHub。
- 远程仓库：`https://github.com/erinamons/DataLens.git`

## README

- 重写 `README.md`。
- 内容包括项目介绍、主要功能、技术栈、本地运行、验证方式、目录结构、环境变量、数据说明和 GitHub 地址。

## 无用文件清理

删除以下不参与运行的文件：

- `web/test_select.html`：独立 select 样式测试页，项目未引用。
- `DataLens_UI升级开发方案.txt`：开发过程方案文档，已过期且运行时未引用。

清理后验证：

```powershell
python -m py_compile main.py server.py database.py config.py test_verify.py
python test_verify.py
```

结果：全部通过。

## 项目记忆

- 新增 `PROJECT_NOTES.md`，用于项目交接说明。
- 根据用户提供的其他项目 memory 案例，新增 `memory/MEMORY.md` 和当天日志 `memory/2026-05-20.md`。
- 后续每次重要功能、结构、运行方式、Git、代理、清理或关键 bug 变更，都要同步更新记忆文件。

## 当前 Git 注意事项

Git 可执行文件路径：

```text
D:\Program Files\Git\cmd\git.exe
```

推送 GitHub 需要代理：

```text
127.0.0.1:9567
```

推送命令：

```powershell
& 'D:\Program Files\Git\cmd\git.exe' -C D:/workbuddy/douyin-data -c http.version=HTTP/1.1 -c http.proxy=http://127.0.0.1:9567 -c https.proxy=http://127.0.0.1:9567 push
```
